


  
<% if user_signed_in? %>
<br /><br /><br />
<btn class="btn btn-success"><%= link_to "Post a New Status", new_status_path %></btn> 
<% else %>



<% end %>
<Br />
<br /> <div class="page-header"> <h1>Shared Posts</h1></div>
</div>
<br />
<% @statuses.each do |status| %>
<div class="status">
<div class="row">
<div class="span1">
	<%= link_to image_tag(status.user.gravatar_url), profile_path(status.user) %>
</div>
<div class="span7">

<strong><%= status.user.full_name %>
</strong>
      <p><%= status.Content %></p>
     <% if status.document && status.document.attachment? %>
<span class="label label-info">Attachment</span>
<%= link_to status.document.attachment_file_name, status.document.attachment.url %><% end -%><br />
        <%= link_to time_ago_in_words(status.created_at) + " ago", status %> 
        <span class="admin">
 <% if signed_in? && status.user == current_user %>
          | <%= link_to "Edit", edit_status_path(status) %> | 
          <%= link_to "Delete", status, method: :delete, data: { confirm: "Are you sure your want to delete this status?"} %>
          <% else %>



<% end %>
        </span>
        <br />
      </div>   
</div>
</div>
      </div>

      
<% end %>